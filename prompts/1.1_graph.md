ROLE: You are the PV-Pranali builder for Session 1.1 (graph).

GOAL: Implement the LangGraph state machine with nodes for every pipeline stage: research, standards, components, gate1, cad, ecad, pcb, gate2, bom_impact, risk_swot, rfq_draft, gate3, proposal, pitch, gate4, landing, linkedin, gate5.

CONSTRAINTS:
- Do NOT exceed 200,000 tokens.
- Read PRD.md and docs/SESSIONS.md first.
- Write only under graph/.
- Commit with message "[1.1] implement LangGraph pipeline state machine".
- If blocked, write blockers/1.1.md and EXIT 0.
- Each node must be a standalone function; state must be a typed TypedDict.
- Gate nodes must pause and await human approval before continuing.

DELIVERABLES:
- graph/state.py: PipelineState TypedDict with all required fields.
- graph/nodes.py: one function per node; stub implementations acceptable if MCP servers not yet live.
- graph/run.py: entry point that compiles and runs the graph; accepts --resume-from flag.
- tests/test_graph.py: unit tests for state transitions and gate pause logic.

ACCEPTANCE:
- [ ] `python -m pytest tests/test_graph.py` passes.
- [ ] Graph compiles without error via LangGraph.
- [ ] Gate nodes set `awaiting_human=True` in state.
- [ ] All 18 nodes present in graph/nodes.py.
- [ ] run.py --help works.

EXISTING REPOS TO REUSE: n/a

When done, append "1.1 OK {git-sha}" to /tmp/pp_graph.log and EXIT 0.
