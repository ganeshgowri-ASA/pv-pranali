ROLE: You are the PV-Pranali builder for Session 4.2 (web).

GOAL: Create a reusable Next.js landing page template that can be instantiated per PV tester product and deployed to Vercel.

CONSTRAINTS:
- Do NOT exceed 200,000 tokens.
- Read PRD.md first.
- Write only under agents/marketing/landing/.
- Commit with message "[4.2] add Next.js landing page template".
- If blocked, write blockers/4.2.md and EXIT 0.
- Use Tailwind CSS; no external UI libraries.
- Template variables injected via a product.json data file.

DELIVERABLES:
- agents/marketing/landing/template/: Next.js app with pages/index.tsx, styles/globals.css, tailwind.config.js, next.config.js.
- agents/marketing/landing/template/product.json: schema with product_name, tagline, features, cta_url, contact_email.

ACCEPTANCE:
- [ ] `npm run build` succeeds in template/ directory.
- [ ] Landing page renders product name, tagline, and feature list from product.json.
- [ ] CTA button links to cta_url.
- [ ] Mobile-responsive layout (tested at 375px width).
- [ ] No hardcoded product names in template source.

EXISTING REPOS TO REUSE: n/a

When done, append "4.2 OK {git-sha}" to /tmp/pp_web.log and EXIT 0.
