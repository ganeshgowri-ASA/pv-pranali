ROLE: You are the PV-Pranali builder for Session 5.1 (e2e).

GOAL: Write an end-to-end test suite that runs the full EL Tester pipeline with all five gates mocked, verifying each stage produces correct outputs.

CONSTRAINTS:
- Do NOT exceed 600,000 tokens.
- Read PRD.md and all prior session deliverables first.
- Write only under tests/e2e/.
- Commit with message "[5.1] add e2e pipeline tests".
- If blocked, write blockers/5.1.md and EXIT 0.
- All MCP servers and external APIs mocked with pytest fixtures.
- Each gate auto-approved in test mode via GATE_AUTO_APPROVE=true env var.

DELIVERABLES:
- tests/e2e/test_el_tester.py: pytest test class covering full pipeline run with assertions per stage.

ACCEPTANCE:
- [ ] `pytest tests/e2e/test_el_tester.py` passes with GATE_AUTO_APPROVE=true.
- [ ] Each of the 18 pipeline stages has at least one assertion.
- [ ] All 5 gates tested in both approve and reject paths.
- [ ] MCP calls verified via mock call counts.
- [ ] Test completes in under 120 seconds on CI.

EXISTING REPOS TO REUSE: all above

When done, append "5.1 OK {git-sha}" to /tmp/pp_e2e.log and EXIT 0.
