ROLE: You are the PV-Pranali builder for Session 0.1 (repo-init).

GOAL: Scaffold the directory tree of the pv-pranali repo and commit a working baseline so subsequent sessions have a place to write code.

CONSTRAINTS:
- Do NOT exceed 50,000 tokens.
- Read PRD.md and docs/SESSIONS.md first; do not duplicate their content.
- Write code under repo root only.
- Commit with message "[0.1] scaffold dirs".
- If blocked, write ./blockers/0.1.md with a one-line reason and EXIT 0 (do NOT loop).
- Do NOT install heavy deps in this session; only create files.

DELIVERABLES:
- Directories: agents/, mcp_servers/, graph/, console/, infra/, docs/, tests/, prompts/, blockers/
- Files:
  - .gitignore (Node + Python + .env + .venv + dist + .next)
  - LICENSE (MIT, owner: Ganesh Gowri / ASA)
  - .env.example with placeholders: MIMO_API_KEY, MIMO_BASE_URL, SUPABASE_URL, SUPABASE_KEY, RAILWAY_TOKEN, MOUSER_API_KEY, DIGIKEY_CLIENT_ID, DIGIKEY_CLIENT_SECRET, GITHUB_TOKEN, VERCEL_TOKEN, UNIPILE_DSN
  - pyproject.toml (uv/poetry stub, name=pv-pranali, python>=3.11)
  - package.json (workspaces: console, graph, mcp_servers/*)
  - infra/token_guard.sh (already documented in PRD; create executable)
  - README.md (overwrite): one-paragraph summary + links to PRD.md and docs/SESSIONS.md and quickstart commands
  - docs/RUNBOOK_WSL.md: step-by-step WSL + tmux + Claude Code + MiMo bring-up

ACCEPTANCE CHECKLIST:
- [ ] All 9 dirs exist with a `.gitkeep` if empty
- [ ] All listed files committed on main
- [ ] `bash -n infra/launch.sh` and `bash -n infra/token_guard.sh` pass
- [ ] README links to PRD + SESSIONS
- [ ] No secrets committed

EXISTING REPOS TO REUSE (reference, do not clone yet):
- ShilpaSutra, antaryami-os, vidyalaya-office, SuryaPrajna, vidyut-srishti-prd, PhotonIQ, spanda-daq, karaveda, karmadhara, surya-yantra, suraksha, SolarLabX, PV-TestEquip-PowerSupply, pareeksha

When done, append a one-line log to /tmp/pp_init.log: "0.1 OK {git-sha}" and EXIT 0.
