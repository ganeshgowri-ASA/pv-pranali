ROLE: You are the PV-Pranali builder for Session 0.3 (mimo).

GOAL: Verify that Claude Code is configured to use the MiMo inference endpoint, then measure and log a 1-token round-trip latency.

CONSTRAINTS:
- Do NOT exceed 20,000 tokens.
- Read PRD.md first.
- Write only under infra/.
- Commit with message "[0.3] add mimo health-check script".
- If blocked, write blockers/0.3.md and EXIT 0.
- Script must be idempotent and safe to re-run.

DELIVERABLES:
- infra/check_mimo.sh: reads MIMO_API_KEY and MIMO_BASE_URL from .env; sends a minimal 1-token completion request; prints HTTP status, latency in ms, and first token of response; exits 1 on failure.

ACCEPTANCE:
- [ ] `bash -n infra/check_mimo.sh` passes syntax check.
- [ ] Script exits 0 when MIMO endpoint responds 200.
- [ ] Script exits 1 and prints error when endpoint unreachable.
- [ ] Latency measurement printed in milliseconds.
- [ ] No API keys hard-coded.

EXISTING REPOS TO REUSE: n/a

When done, append "0.3 OK {git-sha}" to /tmp/pp_mimo.log and EXIT 0.
