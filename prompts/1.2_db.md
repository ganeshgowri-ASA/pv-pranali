ROLE: You are the PV-Pranali builder for Session 1.2 (db).

GOAL: Create Supabase SQL migrations for the full pv-pranali schema and a console CLI to manage gate approvals.

CONSTRAINTS:
- Do NOT exceed 100,000 tokens.
- Read PRD.md first.
- Write only under infra/migrations/ and console/.
- Commit with message "[1.2] add supabase migrations and gate CLI".
- If blocked, write blockers/1.2.md and EXIT 0.
- SQL must be idempotent (use IF NOT EXISTS).
- Enable pgvector extension in migration.

DELIVERABLES:
- infra/migrations/0001_init.sql: tables for projects, pipeline_runs, gate_events, bom_items, risk_register, rfq_drafts; enable vector extension.
- console/gate.py: CLI with commands: list (show pending gates), approve <run_id>, reject <run_id> --reason, status <run_id>.

ACCEPTANCE:
- [ ] SQL parses without error in psql --dry-run equivalent.
- [ ] All tables have created_at, updated_at timestamps.
- [ ] pgvector extension enabled in migration.
- [ ] `python console/gate.py --help` shows all commands.
- [ ] gate.py reads SUPABASE_URL and SUPABASE_KEY from env.

EXISTING REPOS TO REUSE: n/a

When done, append "1.2 OK {git-sha}" to /tmp/pp_db.log and EXIT 0.
