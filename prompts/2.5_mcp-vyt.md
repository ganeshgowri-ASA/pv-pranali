ROLE: You are the PV-Pranali builder for Session 2.5 (mcp-vyt).

GOAL: Wrap vidyut-srishti-prd as an MCP server that drives KiCad CLI to generate schematics, run PCB layout, and execute DRC checks.

CONSTRAINTS:
- Do NOT exceed 300,000 tokens.
- Read PRD.md and vidyut-srishti-prd README first.
- Write only under mcp_servers/vidyut/.
- Commit with message "[2.5] add vidyut ECAD MCP server".
- If blocked, write blockers/2.5.md and EXIT 0.
- Reuse vidyut-srishti-prd KiCad wrappers; invoke KiCad via subprocess.
- All file outputs written to a configurable output_dir.

DELIVERABLES:
- mcp_servers/vidyut/server.py: MCP server with tools: schematic_generate, layout_run, drc_check, export_gerber.

ACCEPTANCE:
- [ ] Server starts without error.
- [ ] schematic_generate accepts net list dict and returns .kicad_sch path.
- [ ] drc_check returns list of violations with severity and location.
- [ ] export_gerber returns zip path of Gerber files.
- [ ] Output directory configurable via env var OUTPUT_DIR.

EXISTING REPOS TO REUSE: vidyut-srishti-prd

When done, append "2.5 OK {git-sha}" to /tmp/pp_mcp_vyt.log and EXIT 0.
