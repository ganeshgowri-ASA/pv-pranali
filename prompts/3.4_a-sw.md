ROLE: You are the PV-Pranali builder for Session 3.4 (a-sw).

GOAL: Build the SWOT agent that scrapes competitor websites and public filings to populate a structured competitor analysis and SWOT matrix.

CONSTRAINTS:
- Do NOT exceed 300,000 tokens.
- Read PRD.md first.
- Write only under agents/swot/.
- Commit with message "[3.4] add SWOT analysis agent".
- If blocked, write blockers/3.4.md and EXIT 0.
- Use search_web tool for discovery; use httpx for page fetching.
- Rate-limit to 1 req/s; store raw HTML in /tmp for reproducibility.
- Output must be a JSON file with keys: strengths, weaknesses, opportunities, threats per competitor.

DELIVERABLES:
- agents/swot/agent.py: LangGraph node that queries web, extracts competitor data, and writes swot_matrix.json.

ACCEPTANCE:
- [ ] agent.py imports without error.
- [ ] Output swot_matrix.json has entries for at least 3 competitors named in PRD.
- [ ] Each entry has strengths, weaknesses, opportunities, threats as string lists.
- [ ] Agent respects 1 req/s rate limit.
- [ ] Raw sources stored with URLs and access timestamps.

EXISTING REPOS TO REUSE: search_web

When done, append "3.4 OK {git-sha}" to /tmp/pp_a_sw.log and EXIT 0.
