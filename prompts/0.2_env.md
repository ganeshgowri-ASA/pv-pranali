ROLE: You are the PV-Pranali builder for Session 0.2 (env).

GOAL: Wire .env.example with all required keys and create shell helpers that validate and provision Supabase, Railway, Vercel, MiMo, Mouser, DigiKey, and Unipile connections.

CONSTRAINTS:
- Do NOT exceed 30,000 tokens.
- Read PRD.md first; do not duplicate its content.
- Write only under infra/ and .env.example.
- Commit with message "[0.2] wire env and setup helpers".
- If blocked, write blockers/0.2.md with a one-line reason and EXIT 0.
- Do not embed real secrets; use placeholder values only.

DELIVERABLES:
- .env.example: full set of required vars with descriptive comments per service group.
- infra/setup_supabase.sh: creates schema, enables pgvector, prints connection string.
- infra/setup_railway.sh: provisions Railway project, sets env vars from .env, prints deploy URL.
- infra/setup_vercel.sh: links repo to Vercel project, sets env vars, prints preview URL.
- infra/check_env.sh: reads .env, asserts all keys non-empty, exits 1 with missing key names if any absent.

ACCEPTANCE:
- [ ] `bash -n infra/setup_supabase.sh` passes.
- [ ] `bash -n infra/setup_railway.sh` passes.
- [ ] `bash -n infra/setup_vercel.sh` passes.
- [ ] `bash -n infra/check_env.sh` passes.
- [ ] .env.example contains keys for MIMO, SUPABASE, RAILWAY, VERCEL, MOUSER, DIGIKEY, UNIPILE.
- [ ] No real credentials committed.

EXISTING REPOS TO REUSE: n/a

When done, append "0.2 OK {git-sha}" to /tmp/pp_env.log and EXIT 0.
