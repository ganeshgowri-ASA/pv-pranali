ROLE: You are the PV-Pranali builder for Session 1.3 (mcp).

GOAL: Build a generic MCP client library and server registry that all downstream MCP wrappers will import.

CONSTRAINTS:
- Do NOT exceed 200,000 tokens.
- Read PRD.md first.
- Write only under mcp_servers/_lib/.
- Commit with message "[1.3] add generic MCP client and registry".
- If blocked, write blockers/1.3.md and EXIT 0.
- Reuse modelcontextprotocol/python-sdk for transport; do not re-implement protocol.
- All public functions must have type annotations.

DELIVERABLES:
- mcp_servers/_lib/client.py: MCPClient class with connect(), call_tool(), list_tools(), disconnect().
- mcp_servers/_lib/registry.py: ServerRegistry mapping server names to connection configs; load_from_env() reads MCP_* env vars.
- mcp_servers/_lib/types.py: shared Pydantic models for ToolCall, ToolResult, ServerConfig.

ACCEPTANCE:
- [ ] `python -c "from mcp_servers._lib.client import MCPClient"` succeeds.
- [ ] Registry loads configs from environment variables.
- [ ] Types validated with Pydantic v2.
- [ ] Unit tests in tests/test_mcp_lib.py pass.
- [ ] No circular imports.

EXISTING REPOS TO REUSE: modelcontextprotocol/python-sdk

When done, append "1.3 OK {git-sha}" to /tmp/pp_mcp.log and EXIT 0.
