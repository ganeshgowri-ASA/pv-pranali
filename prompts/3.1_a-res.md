ROLE: You are the PV-Pranali builder for Session 3.1 (a-res).

GOAL: Build the research agent that scrapes product and pricing data from Pasan, halm, Sinton, Spire, Wavelabs, Endeas, and NISE, then ingests findings into antaryami via the MCP server.

CONSTRAINTS:
- Do NOT exceed 400,000 tokens.
- Read PRD.md and agents/research/ skeleton first.
- Write only under agents/research/.
- Commit with message "[3.1] add research agent".
- If blocked, write blockers/3.1.md and EXIT 0.
- Use browser-use for JS-heavy pages; fall back to httpx for static pages.
- Rate-limit to 1 req/s per domain; respect robots.txt.
- Ingest via antaryami MCP ingest_document tool.

DELIVERABLES:
- agents/research/agent.py: LangGraph-compatible research node; calls scraper and ingestion.
- agents/research/sources.yml: list of target URLs, selectors, and metadata per vendor.

ACCEPTANCE:
- [ ] agent.py imports without error.
- [ ] sources.yml contains entries for all 7 vendors.
- [ ] Scraper respects 1 req/s rate limit.
- [ ] Ingestion calls antaryami MCP ingest_document.
- [ ] Outputs structured JSON with vendor, model, price_usd, specs fields.

EXISTING REPOS TO REUSE: browser-use, antaryami-os

When done, append "3.1 OK {git-sha}" to /tmp/pp_a_res.log and EXIT 0.
