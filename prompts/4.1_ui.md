ROLE: You are the PV-Pranali builder for Session 4.1 (ui).

GOAL: Build a Streamlit console with a Kanban board showing pipeline stages, five gate approval buttons, and a live token-usage meter.

CONSTRAINTS:
- Do NOT exceed 250,000 tokens.
- Read PRD.md first.
- Write only under console/.
- Commit with message "[4.1] add streamlit pipeline console".
- If blocked, write blockers/4.1.md and EXIT 0.
- Gate buttons must call console/gate.py approve/reject via subprocess or direct import.
- Token meter reads from Langfuse or a local SQLite counter.

DELIVERABLES:
- console/app.py: Streamlit app with kanban view, gate buttons, token meter sidebar.
- console/components/kanban.py: reusable Kanban column component.
- console/components/token_meter.py: token usage bar chart component.

ACCEPTANCE:
- [ ] `streamlit run console/app.py` starts without error.
- [ ] Kanban shows all 18 pipeline stages.
- [ ] Gate 1-5 buttons visible and call gate.py logic.
- [ ] Token meter displays used/remaining tokens per session.
- [ ] UI reads pipeline state from Supabase.

EXISTING REPOS TO REUSE: vidyut-eee-portal

When done, append "4.1 OK {git-sha}" to /tmp/pp_ui.log and EXIT 0.
