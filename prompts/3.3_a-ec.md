ROLE: You are the PV-Pranali builder for Session 3.3 (a-ec).

GOAL: Build the ECAD agent that generates KiCad single-line diagrams (SLD), control wiring schematics, and PlantUML P&ID diagrams for PV test equipment.

CONSTRAINTS:
- Do NOT exceed 400,000 tokens.
- Read PRD.md and vidyut-srishti-prd docs first.
- Write only under agents/ecad/.
- Commit with message "[3.3] add ECAD agent".
- If blocked, write blockers/3.3.md and EXIT 0.
- Drive KiCad via vidyut MCP server; drive PlantUML via subprocess.
- All outputs written to output_dir passed in state.

DELIVERABLES:
- agents/ecad/sld.py: generate_sld() function; calls vidyut MCP schematic_generate.
- agents/ecad/pid.py: generate_pid() function; renders PlantUML source and exports PNG/SVG.

ACCEPTANCE:
- [ ] generate_sld() returns path to .kicad_sch file.
- [ ] generate_pid() returns path to PNG and SVG files.
- [ ] Both functions accept PipelineState and return updated state.
- [ ] PlantUML source stored alongside output for reproducibility.
- [ ] Unit tests pass with mocked MCP calls.

EXISTING REPOS TO REUSE: KiCad, PlantUML

When done, append "3.3 OK {git-sha}" to /tmp/pp_a_ec.log and EXIT 0.
